<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sale Details - ElectroLand</title>
  <style>
    /* Structural CSS */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #f5f6fa;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .container {
      margin-top: 90px;
      display: flex;
      flex: 1;
      gap: 25px; /* Standardized gap */
      padding: 0 20px !important; /* Standardized padding with !important to prevent override */
      max-width: 100%; /* Ensure it doesn’t exceed viewport */
    }

    .content-area {
      background: #ffffff;
      border-radius: 15px;
      padding: 20px; /* Standardized padding */
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08); /* Standardized shadow */
      flex: 1;
      overflow-x: auto; /* Horizontal scroll if needed */
      overflow-y: auto; /* Vertical scroll for overflow content */
      height: calc(100vh - 90px); /* Match sidebar height exactly */
    }

    h2 {
      color: #2d3436;
      margin-bottom: 20px; /* Standardized margin */
      font-size: 1.5rem; /* Standardized font size */
    }

    h3 {
      color: #2d3436;
      margin: 20px 0 10px;
      font-size: 1.2rem; /* Consistent with section titles */
    }

    .form-group {
      margin-bottom: 15px;
    }

    .form-group label {
      display: block;
      margin-bottom: 5px;
      color: #2d3436; /* Updated to match other labels */
      font-weight: 500; /* Consistent with other forms */
    }

    .form-group input {
      width: 100%;
      padding: 8px; /* Standardized padding */
      border: 1px solid #e6e9f0; /* Consistent border color */
      border-radius: 5px;
      font-size: 0.9rem; /* Standardized font size */
      background: #f8f9fa; /* Kept original background */
      color: #2d3436;
      transition: border-color 0.3s; /* Added for consistency */
    }

    .form-group input:focus {
      border-color: #333333;
      outline: none;
    }

    .form-row {
      display: flex;
      gap: 15px; /* Standardized gap */
      flex-wrap: wrap;
    }

    .form-row .form-group {
      flex: 1 1 45%; /* Two columns by default, consistent with previous forms */
      min-width: 200px; /* Consistent min-width */
      max-width: 400px; /* Kept original max-width */
    }

    .button {
      display: inline-block;
      padding: 10px 20px; /* Standardized button padding */
      background-color: #333333; /* Consistent button color */
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 0.9rem; /* Standardized font size */
      cursor: pointer;
      transition: background-color 0.3s;
      width: 100%;
      margin-top: 20px;
    }

    .button:hover {
      background-color: #555555; /* Consistent hover color */
    }

    .back-link {
      display: block;
      margin-top: 15px; /* Adjusted for consistency */
      color: #2d3436; /* Updated to match other links */
      text-decoration: none;
      font-size: 0.9rem; /* Standardized font size */
      text-align: center; /* Added for better presentation */
    }

    .back-link:hover {
      text-decoration: underline;
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
      .container {
        flex-direction: column;
        padding: 0 10px !important; /* Consistent responsive padding */
      }

      .content-area {
        padding: 15px;
        height: auto; /* Switch to auto height on smaller screens */
      }

      h2 {
        font-size: 1.2rem;
      }

      h3 {
        font-size: 1.1rem;
      }

      .form-row {
        flex-direction: column;
      }

      .form-row .form-group {
        flex: 1 1 100%; /* Stack fields on smaller screens */
      }

      .button {
        padding: 8px;
        font-size: 0.85rem;
      }
    }

    @media (min-width: 769px) and (max-width: 1024px) {
      h2 {
        font-size: 1.3rem;
      }

      .form-row .form-group {
        flex: 1 1 45%;
      }
    }
  </style>
</head>
<body>
  <%- include('../../partials/navbar') %>

  <div class="container">
    <%- include('../../partials/admin_sidebar') %>

    <!-- Content Area -->
    <div class="content-area" id="content">
      <h2>Sale Details</h2>

      <h3>Sale Information</h3>
      <div class="form-row">
        <div class="form-group">
          <label>Sale ID</label>
          <input type="text" value="<%= sale.sales_id %>" readonly>
        </div>
        <div class="form-group">
          <label>Unique Code</label>
          <input type="text" value="<%= sale.unique_code %>" readonly>
        </div>
        <div class="form-group">
          <label>Sale Date</label>
          <input type="text" value="<%= sale.sales_date %>" readonly>
        </div>
      </div>
      <div class="form-row">
        <div class="form-group">
          <label>Branch Name</label>
          <input type="text" value="<%= sale.branch_name %>" readonly>
        </div>
        <div class="form-group">
          <label>Salesman Name</label>
          <input type="text" value="<%= sale.salesman_name %>" readonly>
        </div>
        <div class="form-group">
          <label>Customer Name</label>
          <input type="text" value="<%= sale.customer_name %>" readonly>
        </div>
      </div>

      <h3>Product Information</h3>
      <div class="form-row">
        <div class="form-group">
          <label>Product Name</label>
          <input type="text" value="<%= sale.product_name %>" readonly>
        </div>
        <div class="form-group">
          <label>Model Number</label>
          <input type="text" value="<%= sale.model_number %>" readonly>
        </div>
        <div class="form-group">
          <label>Company Name</label>
          <input type="text" value="<%= sale.company_name %>" readonly>
        </div>
      </div>

      <h3>Transaction Details</h3>
      <div class="form-row">
        <div class="form-group">
          <label>Purchased Price</label>
          <input type="text" value="$<%= sale.purchased_price %>" readonly>
        </div>
        <div class="form-group">
          <label>Sold Price</label>
          <input type="text" value="$<%= sale.sold_price %>" readonly>
        </div>
        <div class="form-group">
          <label>Quantity</label>
          <input type="text" value="<%= sale.quantity %>" readonly>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label>Total Amount</label>
          <input type="text" value="$<%= sale.amount %>" readonly>
        </div>
        <div class="form-group">
          <label>Profit/Loss</label>
          <input type="text" value="$<%= sale.profit_or_loss %>" readonly>
        </div>
      </div>

      <a href="/admin/sales" class="back-link">Back to Sales</a>
    </div>
  </div>

  <%- include('../../partials/footer') %>
</body>
</html>