<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sale Details - ElectroLand</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #f5f6fa;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .container {
      margin-top: 90px;
      display: flex;
      flex: 1;
      gap: 25px;
      padding: 0 20px;
    }

    .content-area {
      background: #ffffff;
      border-radius: 15px;
      padding: 25px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      flex: 1;
    }

    .content-area h1 {
      color: #2d3436;
      margin-bottom: 20px;
    }

    .edit-button {
      display: inline-block;
      padding: 10px 20px;
      background-color: #2d3436;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      margin-top: 20px;
    }

    .edit-button:hover {
      background-color: #FFD700;
      color: #2d3436;
    }

    input[readonly], select[readonly], textarea[readonly] {
      background-color: #e9ecef;
      cursor: not-allowed;
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
        padding: 0 20px;
      }

      .content-area {
        min-height: auto;
      }
    }
  </style>
</head>
<body>
  <%- include('../../partials/navbar') %>

  <div class="container">
    <%- include('../../partials/salesmanager_sidebar') %>

    <div class="content-area" id="content">
      <div class="form-container">
        <h2>Sale Details</h2>
        <form>
          <fieldset>
            <legend>Sale Information</legend>
            <div class="field-group">
              <div>
                <label for="sales_id">Sale ID</label>
                <input type="text" id="sales_id" value="<%= sale.sales_id %>" readonly>
              </div>
              <div>
                <label for="branch_name">Branch</label>
                <input type="text" id="branch_name" value="<%= sale.branch_name %>" readonly>
              </div>
              <div>
                <label for="salesman_name">Salesman</label>
                <input type="text" id="salesman_name" value="<%= sale.salesman_name %>" readonly>
              </div>
              <div>
                <label for="customer_name">Customer</label>
                <input type="text" id="customer_name" value="<%= sale.customer_name %>" readonly>
              </div>
              <div>
                <label for="product_name">Product</label>
                <input type="text" id="product_name" value="<%= sale.product_name %>" readonly>
              </div>
              <div>
                <label for="quantity">Quantity</label>
                <input type="number" id="quantity" value="<%= sale.quantity %>" readonly>
              </div>
              <div>
                <label for="total_amount">Total Amount</label>
                <input type="number" step="0.01" id="total_amount" value="<%= sale.total_amount %>" readonly>
              </div>
              <div>
                <label for="saledate">Sale Date</label>
                <input type="text" id="saledate" value="<%= sale.saledate %>" readonly>
              </div>
              <div>
                <label for="price">Price</label>
                <input type="number" step="0.01" id="price" value="<%= sale.price %>" readonly>
              </div>
              <div>
                <label for="phone_number">Phone Number</label>
                <input type="tel" id="phone_number" value="<%= sale.phone_number %>" readonly>
              </div>
            </div>
          </fieldset>
        </form>
      </div>
    </div>
  </div>

  <%- include('../../partials/footer') %>
</body>
</html>